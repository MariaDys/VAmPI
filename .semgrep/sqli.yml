# .semgrep/sqli-variable.yml
rules:
  - id: sqli-fstring-variable
    message: "Поймали SQLi через f-строку в переменной"
    languages: [python]
    severity: ERROR
    patterns:
      - pattern: |
          $Q = f"...{...}..."
      - pattern-inside: |
          db.session.execute(text($Q))
