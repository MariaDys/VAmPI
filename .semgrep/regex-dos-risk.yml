rules:
  - id: regex-dos-risk
    message: |
      Возможен ReDoS: тяжёлый вызов `re.search` со сложным регулярным выражением.
      Необходимо упростить паттерн или ограничить длину входа.
    languages: [python]
    severity: WARNING
    pattern-regex: >
      re\.search\([\s\S]*r"[^"]{20,}"[\s\S]*\)
