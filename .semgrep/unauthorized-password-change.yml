# .semgrep/unauthorized-password-change.yml
rules:
  - id: unauthorized-password-change
    message: |
      Несанкционированное изменение пароля:
      пароль присваивается без проверки прав текущего пользователя.
      username должен быть из токена, а не из URL-параметра.
    languages: [python]
    severity: ERROR
    pattern: |
      user = User.query.filter_by(username=$USERNAME).first()
      ...
      user.password = request_data.get('password')
      ...
      db.session.commit()
